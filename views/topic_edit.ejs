<%- include head.ejs %>

<body>
	<%- include navbar.ejs %>
	
	<div class="container-fluid">
		<div class="span3">
			<%- include sidebar.ejs %>
			<div class="area-title">话题编辑提示</div>
			<div class="area-content">
				<div class="area-content-inner">
					<p>我其实是侧边栏啊啊啊啊啊左侧边栏哦呵呵呵提示输入那种</p>
					<p>markdown到底是个啥？看起来好高端的样子。目前还不支持的55555。</p>
				</div>
			</div>
		</div>
		
		<div class="span9">
			<div class="area-title">
				<ul class="breadcrumb">
					<li><a href="/">主页</a><span class="divider"> / </span></li>
					<% if (typeof(action) !== 'undefined') { %>
					<li class="active">编辑话题</li>
					<% } else { %>
					<li class="active">发布话题</li>
					<% } %>
				</ul>
			</div>
			<div class="area-content">
				<div class="area-content-inner">
					<% if (typeof(action) !== 'undefined' && action === 'edit') { %>
						<form id="create_topic_form" action="/topic/<%= topic_id %>/edit" method="post">
					<% } else { %>
						<form id="create_topic_form" action="/topic/add" method="post">
					<% } %>
						<textarea autofocus class="span9" id="topic-title" name="title" rows="2">
							<%= typeof(title) !== 'undefined' && title || '' %>
						</textarea>
						<div class="markdown-editer in-editer">
							<div class="markdown-in-editor">
								<textarea class="editor span9" name="content" rows="20">
									<%= typeof(content) !== 'undefined' && content || "" %>
								</textarea>						
							</div>
						</div>	
						<button type="submit" class="btn btn-info">提交</button>							
					</form>
				</div>
			</div>
		</div>		
	</div>
	<%- include footer.ejs %>	
</body>
  <%- include scripts.ejs %>
</html>
